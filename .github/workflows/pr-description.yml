name: Auto-populate PR Description

# This workflow automatically populates PR descriptions from PULL-REQUEST-DETAILS.md
# only if the PR description is empty. It preserves manually written descriptions.

on:
  pull_request:
    types: [opened, reopened]

jobs:
  populate-description:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for PULL-REQUEST-DETAILS.md
        id: check-file
        run: |
          if [ -f "PULL-REQUEST-DETAILS.md" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Populate PR description (only if empty)
        if: steps.check-file.outputs.exists == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            // Read the file content
            const content = fs.readFileSync('PULL-REQUEST-DETAILS.md', 'utf8');

            const { data: pullRequest } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number
            });

            const currentBody = pullRequest.body || '';

            // Only update if the PR description is empty or null
            if (!currentBody || currentBody.trim() === '') {
              await github.rest.pulls.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: context.issue.number,
                body: content
              });
              console.log('PR description populated successfully');
            } else {
              console.log('PR description is not empty, skipping update');
            }
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
