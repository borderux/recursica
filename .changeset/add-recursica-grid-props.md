---
"@recursica/adapter-common": minor
---

Added `RecursicaGridProps` — a formal prop contract for Grid layout components exposing `columns` as a token-backed override, matching the pattern `RecursicaContainerProps.size` already uses.
