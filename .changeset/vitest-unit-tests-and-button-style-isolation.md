---
"@recursica/mantine-adapter": patch
"@recursica/mui-adapter": patch
---

Wired vitest into `npm test` (unit tests for utils). Added a Button/kit CSS-isolation DOM test, run separately via `npm run test:dom` (not part of `npm test`/CI). Added empty test scaffolds per component.
