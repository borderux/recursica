---
"@recursica/adapter-common": patch
---

Add `FileUpload`, `Tree`, `CheckboxGroup`, `Box`, and `Typography` to `RECURSICA_COMPONENTS` — all are real, shipped components that were missing from the registry.
