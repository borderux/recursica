---
"@recursica/mantine-adapter": minor
"@recursica/mui-adapter": minor
"@recursica/adapter-common": patch
---

Implement `TimePicker` (previously a stub) in both adapters: `@mantine/dates` `TimeInput` for mantine-adapter, `@mui/x-date-pickers` `TimePicker` for mui-adapter.
