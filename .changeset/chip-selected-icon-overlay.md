---
"@recursica/mantine-adapter": patch
"@recursica/mui-adapter": patch
---

Chip's selected checkmark now overlays the leading icon instead of sitting to its left, matching MUI's native behavior (mui-adapter already did this; mantine-adapter now matches).
