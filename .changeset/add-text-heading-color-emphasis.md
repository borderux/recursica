---
"@recursica/adapter-common": minor
---

Added optional `color` (`default` | `warning` | `alert` | `success`, defaults to `default`) and `emphasis` (`low` | `high`, defaults to `high`) props to `RecursicaTextProps` and `RecursicaHeadingProps`.
