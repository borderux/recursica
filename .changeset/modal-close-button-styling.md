---
"@recursica/mantine-adapter": patch
"@recursica/mui-adapter": patch
---

Modal close button now matches the Button component's styling (radius, padding, color tokens, hover/focus states, icon sizing).
