---
"@recursica/mantine-adapter": patch
"@recursica/mui-adapter": patch
---

Added `text-wrap: balance` to `Text` and `Title` so wrapped multi-line text (headings and paragraphs) gets more even line lengths.
