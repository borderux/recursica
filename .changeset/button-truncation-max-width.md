---
"@recursica/mantine-adapter": patch
"@recursica/mui-adapter": patch
---

Fixed Button ignoring a constraining parent width — it now shrinks and truncates the label with an ellipsis instead of overflowing.
