---
"@recursica/mantine-adapter": patch
"@recursica/mui-adapter": patch
---

Fix Tree's chevron button stealing DOM focus on click while `aria-hidden`, which browsers flag as an accessibility violation.
