---
"@recursica/mantine-adapter": patch
"@recursica/mui-adapter": patch
"@recursica/adapter-common": patch
---

Fixed `Table` currency columns so header and footer cells right-align to match value cells. `Table.Th`/`Table.Cell` header context now supports `variant="currency"`.
