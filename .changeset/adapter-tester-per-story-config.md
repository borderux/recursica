---
"@recursica/adapter-tester": major
---

`storyThresholds`/`excludeStoryIds` config fields replaced with a single `stories` map keyed by story id prefix, each entry holding `{ threshold?, exclude? }`.
