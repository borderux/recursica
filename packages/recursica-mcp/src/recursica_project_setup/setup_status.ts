export const setup_status = `✅ **Status**: **\`@recursica/{{cleanName}}-adapter\`** is already successfully installed in your project!

Recommended Next Step:
Call the tool **\`recursica_get_usage\`** with \`adapter: "{{cleanName}}"\` to view general styling guidelines, layout spacing tokens, and usage rules.`;
