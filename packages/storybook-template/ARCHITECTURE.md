# Architecture

## Overview

This document describes the high-level architecture of the `@recursica/storybook-template` package.

## Dependencies

- **Storybook**: Core component development framework.

## Key Design Decisions

- Provides shared Storybook configuration, decorators, and templates so we don't have to duplicate `.storybook` setup across multiple apps.
