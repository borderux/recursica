# Architecture

## Overview

This document describes the high-level architecture of the `@recursica/adapter-common` package.

## Dependencies

- **React**: Core UI rendering engine.

## Key Design Decisions

- Contains framework-agnostic logic and hooks to prevent duplication across specific adapters (Mantine, MUI, etc).
