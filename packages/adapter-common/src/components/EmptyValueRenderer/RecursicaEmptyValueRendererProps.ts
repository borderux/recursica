import React from "react";

/**
 * Props for the Recursica EmptyValueRenderer component.
 */
export interface RecursicaEmptyValueRendererProps {
  /** Fallback node to show when values are missing */
  fallback?: React.ReactNode;
}
