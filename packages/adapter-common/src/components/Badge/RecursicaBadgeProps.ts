/**
 * Props for the Recursica Badge component.
 */
export interface RecursicaBadgeProps {
  /** Map to the component styles defined in variables */
  variant?: "alert" | "primary-color" | "success" | "warning";
}
