# Architecture

## Overview

This document describes the high-level architecture of the `@recursica/recursica-postcss-vars` package.

## Dependencies

- **PostCSS**: Used as the parser to analyze CSS files.

## Key Design Decisions

- Enforces strict validation of CSS variables against a provided tokens JSON file to ensure design system consistency.
