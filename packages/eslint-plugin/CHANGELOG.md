# eslint-plugin-recursica

## 0.2.0

### Minor Changes

- 0efd251: First version release. Adds `no-over-styled` rule, flagging use of the `overStyled` escape-hatch prop on Recursica components (error by default).
