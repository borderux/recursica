import bundleData from "./recursica-bundle.json";
import type { Recursica } from "./recursica";

// Export the raw JSON data as a Recursica object
const recursicaBundle: any = bundleData;

export { recursicaBundle };
