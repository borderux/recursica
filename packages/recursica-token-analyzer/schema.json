{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Token Analysis Report",
  "description": "Report generated by @recursica/token-analyzer detailing the usage of CSS variables across adapter components.",
  "type": "object",
  "required": [
    "summary",
    "components",
    "brokenComponents",
    "missingVariables",
    "unusedByComponent",
    "staleExemptions"
  ],
  "properties": {
    "summary": {
      "type": "object",
      "description": "High-level summary of CSS variable health in the analyzed project.",
      "required": [
        "totalDefined",
        "totalUsed",
        "totalMissing",
        "totalUnused",
        "totalStaleExemptions"
      ],
      "properties": {
        "totalDefined": {
          "type": "integer",
          "description": "Total number of CSS variables explicitly defined in the scoped UI Kit dictionary."
        },
        "totalUsed": {
          "type": "integer",
          "description": "Total number of CSS variables consumed within the adapter's component source files."
        },
        "totalMissing": {
          "type": "integer",
          "description": "Number of variables referenced by components that do not exist in the scoped UI Kit dictionary. These represent broken dependencies."
        },
        "totalUnused": {
          "type": "integer",
          "description": "Number of variables defined in the scoped UI Kit dictionary that are not consumed by any component. These represent unmapped features or states."
        },
        "totalStaleExemptions": {
          "type": "integer",
          "description": "Number of recursica-ignore directives that reference a CSS variable no longer defined in the UI Kit dictionary at all."
        }
      }
    },
    "components": {
      "type": "object",
      "description": "Master dictionary mapping lowercase component IDs to their source directory and files.",
      "additionalProperties": {
        "type": "object",
        "required": ["name", "tokenPrefix", "directory", "files"],
        "properties": {
          "name": {
            "type": "string",
            "description": "The human-readable name of the component."
          },
          "tokenPrefix": {
            "type": "string",
            "description": "The string identifier used to match Figma CSS variables to this component (e.g. 'button' for '--recursica_ui-kit_components_button_...')."
          },
          "directory": {
            "type": "string",
            "description": "The relative path to the component's root directory."
          },
          "files": {
            "type": "array",
            "description": "Array of source files associated with this component.",
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "brokenComponents": {
      "type": "array",
      "description": "List of component IDs that reference at least one missing CSS variable.",
      "items": {
        "type": "string"
      }
    },
    "missingVariables": {
      "type": "array",
      "description": "Detailed list of broken CSS variable references and the component attempting to use them.",
      "items": {
        "type": "object",
        "required": ["variable", "componentId", "files"],
        "properties": {
          "variable": {
            "type": "string",
            "description": "The exact name of the CSS variable that is missing from the UI Kit dictionary."
          },
          "componentId": {
            "type": "string",
            "description": "The lowercase ID of the component that referenced the broken variable."
          },
          "files": {
            "type": "array",
            "description": "Array of file paths where this missing CSS variable is referenced.",
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "unusedByComponent": {
      "type": "object",
      "description": "Dictionary grouping unused CSS variables by their associated component ID.",
      "additionalProperties": {
        "type": "array",
        "description": "List of unused CSS variables belonging to this component namespace.",
        "items": {
          "type": "string"
        }
      }
    },
    "staleExemptions": {
      "type": "array",
      "description": "Detailed list of recursica-ignore directives that reference a CSS variable no longer defined in the UI Kit dictionary at all (as opposed to a variable that still exists but is simply unused). Pass --cleanup to remove these automatically.",
      "items": {
        "type": "object",
        "required": ["variable", "componentId", "filePath", "line"],
        "properties": {
          "variable": {
            "type": "string",
            "description": "The exact name of the CSS variable the stale directive references."
          },
          "componentId": {
            "type": "string",
            "description": "The lowercase ID of the component whose file contains the stale directive."
          },
          "filePath": {
            "type": "string",
            "description": "The file containing the stale recursica-ignore directive."
          },
          "line": {
            "type": "integer",
            "description": "The 1-indexed line number of the stale recursica-ignore directive within the file."
          }
        }
      }
    }
  }
}
